(function(){
  
    let x:string = '0'; //para guardar cadenas de caracteres, texto literal
    let y:boolean = true; //para guardar booleanos true o false y tal
    let z:number = 12.3; //number para guardar numeros enteros o decimales
    let hoy:Date = new Date; //tipo Date para guardar fechas

    /**
     * el tipo de variable "any" guarda cualquier tipo de dato, sin especificar
     * "|" restringe el tipo de dato que puede contener la variable any
     * let cualquiercosa:string | number;
     */

    //crear objetos, dentro de la llave se ponen los atributos
    let obj = {
        nombre: 'obj',
        edad: 14,
    }
})